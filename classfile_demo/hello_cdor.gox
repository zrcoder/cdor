import "os"

cons(
	con("Go+", "Go").label("cdor"),

//	con("x", "y").
//		stroke("red").
//		srcOpt(opt.label("from").shape("arrow")).
//		dstOpt(opt.label("to").shape("diamond")),
)

data, err := gen
if err != nil {
	echo "err:", err
	return
}

os.writeFile "hello.svg", data, 0600
